<?xml version="1.0" encoding="UTF-8"?>
<server description="PostWizardREST">

    <!-- Enable features -->
    <featureManager>
        <feature>jakartaee-10.0</feature>
        <feature>jdbc-4.2</feature>
        <feature>jndi-1.0</feature>
        <feature>localConnector-1.0</feature>
    </featureManager>

    <!-- This template enables security. To get the full use of all the capabilities, a keystore and user registry are required. -->

    <!-- For the keystore, default keys are generated and stored in a keystore. To provide the keystore password, generate an 
        encoded password using bin/securityUtility encode and add it below in the password attribute of the keyStore element. 
        Then uncomment the keyStore element. -->
    <!--
    <keyStore password=""/> 
    -->

    <!--For a user registry configuration, configure your user registry. For example, configure a basic user registry using the
        basicRegistry element. Specify your own user name below in the name attribute of the user element. For the password,
        generate an encoded password using bin/securityUtility encode and add it in the password attribute of the user element.
        Then uncomment the user element. -->
   <basicRegistry id="dummyRegistry">
    <user name="placeholder" password="placeholder"/>
   </basicRegistry>

    <!-- To access this server from a remote client add a host attribute to the following element, e.g. host="*" -->
    <httpEndpoint id="defaultHttpEndpoint"
                  httpPort="9080"
                  httpsPort="9443" />

    <!-- Automatically expand WAR files and EAR files -->
    <applicationManager autoExpand="true"/>

    <!-- Configures the application on a specified context root -->
   <webApplication contextRoot="/" location="${server.config.dir}/apps/PostWizardREST.war" />

    <!-- Default SSL configuration enables trust for default certificates from the Java runtime -->
    <ssl id="defaultSSLConfig" trustDefaultCerts="true" />
    
    <cdi enableImplicitBeanArchives="true" emptyBeansXmlCDI3Compatibility="true"/>
    <applicationMonitor updateTrigger="mbean"/>
    
    <library id="mariadbDriver">
    <fileset dir="${server.config.dir}/jdbc" includes="mariadb-java-client-*.jar"/>
    </library>
    
    <!-- These are dummy credentials -->
    <dataSource id="WordPressLocal" jndiName="jdbc/wm_test" type="javax.sql.DataSource" validationTimeout="30s" >
	<jdbcDriver libraryRef="mariadbDriver"
               javax.sql.DataSource="org.mariadb.jdbc.MariaDbDataSource"/>
        <properties
            url="jdbc:mariadb://localhost:3306/wm_test"
            user="${env.DB_USER}"
            password="${env.DB_PASSWORD}"/>
        <connectionManager
            maxPoolSize="20"
            minPoolSize="5"
            connectionTimeout="60s"
	    purgePolicy="ValidateAllConnections"
	    autoCloseConnections="false"/>
    </dataSource>
    
</server>
